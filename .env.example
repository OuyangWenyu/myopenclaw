# =============================================================
# 全局环境变量模板 — 复制为 .env 并填写真实值
# =============================================================

# ── 通用 ─────────────────────────────────────────────────────
TZ=Asia/Shanghai

# ── hermes ───────────────────────────────────────────────────
HERMES_PORT=8642
HERMES_DASHBOARD_PORT=9119

# ── openclaw ─────────────────────────────────────────────────
OPENCLAW_PORT=18789
# OPENCLAW_IMAGE=ghcr.io/openclaw/openclaw:latest   # 使用预构建镜像

# ── 备份（backup-cron 容器）─────────────────────────────────
# cron 表达式：默认每周日凌晨 2:00 备份一次
BACKUP_CRON=0 2 * * 0
# 快照保留天数（超过后自动删除旧快照）
BACKUP_KEEP_DAYS=30
# BACKUP_ROOT 由 docker-compose 从 .cloud.conf 解析后注入，无需手动填写
