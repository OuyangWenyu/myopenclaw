[project]
name = "myopenclaw"
version = "0.1.0"
description = "个人多 Agent 协作平台 — Docker Compose 部署文档"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
docs = [
    "mkdocs-material>=9.6",
]

[tool.uv]
package = false
